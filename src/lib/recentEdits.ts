// Re-exports the generated map (`recentEdits.generated.ts`). A committed
// placeholder keeps `tsc` green before `prebuild`; the script overwrites it locally.
export { RECENT_EDITS } from './recentEdits.generated'
